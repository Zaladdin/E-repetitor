# Browser API URL (use the same hostname for web and API so cookies work).
NEXT_PUBLIC_API_URL=http://127.0.0.1:4000/api/v1
# Local Docker PostgreSQL only. Replace before starting Compose.
POSTGRES_PASSWORD=replace-with-a-random-local-password
